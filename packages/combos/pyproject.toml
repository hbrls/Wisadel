[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "combos"
version = "0.1.0"

[tool.setuptools]
packages = ["combos"]

[tool.setuptools.package-dir]
"combos" = "."
