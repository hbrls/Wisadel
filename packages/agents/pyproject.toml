[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "agents"
version = "0.1.0"

[tool.setuptools]
packages = ["agents", "agents.agents", "agents.providers", "agents.tools"]

[tool.setuptools.package-dir]
"agents" = "."
